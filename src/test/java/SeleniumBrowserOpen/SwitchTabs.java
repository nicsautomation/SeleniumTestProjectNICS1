package SeleniumBrowserOpen;

import java.util.Iterator;
import java.util.Set;

import org.openqa.selenium.By;

public class SwitchTabs {

	public static void main(String[] args) throws InterruptedException {

		/*
		 * startSession("https://www.online.citibank.co.in/"); Set<String> windowsH =
		 * getDriver().getWindowHandles(); Iterator<String> itr = windowsH.iterator();
		 * String window1H = itr.next(); System.out.println(window1H);
		 * click(getDriver().findElement(By.xpath("//img[@class=\"appClose\"]")));
		 * click(getDriver().findElement(By.xpath("//img[@alt='Branch/ATM']")));
		 * click(getDriver().findElement(By.xpath("//span[@class='txtSign']")));
		 * windowsH = getDriver().getWindowHandles(); itr = windowsH.iterator();
		 * System.out.println(itr.next()); String window2H = itr.next();
		 * getDriver().switchTo().window(window2H);
		 * sendkeys(getDriver().findElement(By.xpath("//input[@name=\"User_Id\"]")),
		 * "ABCD"); Thread.sleep(3000); tearDown();
		 */
	}
}
